A base module to create a Cloud Function with a HTTP trigger and lauch eks service simultaneously. Also creates a storage bucket and zips and uploads the code from a given path.

Terraform Version: 0.12
